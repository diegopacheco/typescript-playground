enum Direction {
  Up = 1,
  Down = "Down",
  Left = "Left",
  Right = Down
}

console.log("The only way is to go: " + Direction.Up);
console.log("The only way is to go: " + Direction.Down);
console.log("The only way is to go: " + Direction.Left);
console.log("The only way is to go: " + Direction.Right);
